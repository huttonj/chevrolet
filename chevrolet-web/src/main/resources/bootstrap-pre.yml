spring:
  application:
    name: chevrolet
  cloud:
    config:
      profile: pre
      name: chevrolet
      discovery:
        enabled: true
        serviceId: lorinser
      enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://10.0.1.3:8761/eureka/,http://10.0.1.4:8761/eureka/
    enabled: true
